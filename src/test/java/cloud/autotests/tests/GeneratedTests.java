package cloud.autotests.tests;

import cloud.autotests.helpers.DriverUtils;
import io.qameta.allure.Description;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static com.codeborne.selenide.Selenide.open;
import static com.codeborne.selenide.Selenide.title;
import static io.qameta.allure.Allure.step;
import static org.assertj.core.api.Assertions.assertThat;


public class GeneratedTests extends TestBase {
    @Test
    @Description("Soon to be implemented by you (or QA.GURU engineers)")
    @DisplayName("succesfull test vtb")
    void generatedTest() {
        step("Open https://www.vtb.ru/", () -> {
            step("// todo: just add selenium action");
        });

        step("Accept cookies notification", () -> {
            step("// todo: just add selenium action");
        });

        step("Click second carousel slide", () -> {
            step("// todo: just add selenium action");
        });

        step("Click on button more", () -> {
            step("// todo: just add selenium action");
        });

        step("Click on button issue a card", () -> {
            step("// todo: just add selenium action");
        });

        step("Click on card android", () -> {
            step("// todo: just add selenium action");
        });

        step("Enter first and last name", () -> {
            step("// todo: just add selenium action");
        });

        step("Enter birthday", () -> {
            step("// todo: just add selenium action");
        });

        step("Enter email", () -> {
            step("// todo: just add selenium action");
        });

        step("Enter mobile number", () -> {
            step("// todo: just add selenium action");
        });

        step("Check should attribute on button disabled", () -> {
            step("// todo: just add selenium action");
        });
    }
}